<div class="container mt-5">
    <h1 class="mb-4">Tu Perfil (sin datos sensibles)</h1>
    <div class="card">
        <div class="card-body">
            <p class="card-text"><strong>Nombre:</strong> {{user.name}}</p>
            <p class="card-text"><strong>Email:</strong> {{user.email}}</p>
            <p class="card-text"><strong>Edad:</strong> {{user.age}}</p>
            <p class="card-text"><strong>Rol:</strong> {{user.role}}</p>
        </div>
        <button id="logoutButton" onclick="logout()" class="btn btn-danger">Cerrar Sesión</button>
    </div>
</div>
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script src="../public/profile.js"></script>

<script>console.log("{{user.role}}")</script>